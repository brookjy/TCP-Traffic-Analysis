A simple Tracer file using c capture programming:

TraceParser.c:
This file gets all the capture data from the capture file, seted to be off_line. The program extract the file and get the data from each connection. then it calculate the states and RTT .etc. And generate the report. 

The file "cap-feb-6.cap" is the target file that for testing the program.